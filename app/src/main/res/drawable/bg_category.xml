<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Nền vàng nhạt -->
    <solid android:color="@color/yellow_slight" />

    <!-- Bo tròn nhẹ các góc -->
    <corners android:radius="15dp" />

    <!-- Viền: dày 1dp, màu vàng đậm -->
    <stroke
        android:width="1dp"
        android:color="@color/yellow_bold" />
</shape>
